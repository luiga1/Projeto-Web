const botao = document.getElementById('butao');
const azedo = document.getElementById('azedu');
const body = document.body;

botao.addEventListener('click', () => {

    azedo.classList.toggle('hidden');
    
});